#include "main.h"

/**
 * add - add 2 integers then returns the result of them
 * @a: first integer
 * @b: second integer
 *
 * Return: the sum of a and b
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
