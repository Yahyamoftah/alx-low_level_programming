#include "main.h"

/**
 * _abs - compute absolute value of the integer
 * @n: integar
 *
 * Return: returns absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

